# LogLineOS Environment Configuration

# Security - API Authentication
# CRITICAL: Change these values in production!
API_KEY=your-secure-api-key-here-minimum-8-chars
JWT_SECRET=your-jwt-secret-minimum-32-characters-long

# Cryptographic Keys (Ed25519)
# Generate with: deno run -A tools/cli/logline-cli.ts generate-keys
SIGNING_KEY_HEX=
PUBLIC_KEY_HEX=

# Database Configuration (Optional - for PostgreSQL adapter)
DATABASE_URL=postgresql://user:password@localhost:5432/loglineos
DB_POOL_SIZE=10
DB_TIMEOUT=5000

# Ledger Configuration
LEDGER_PATH=./data/ledger.jsonl
LEDGER_ROTATION=monthly
LEDGER_MAX_SIZE=104857600

# Security Settings
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100

# Observability
LOG_LEVEL=info
METRICS_PORT=9090
TRACING_ENABLED=false

# Server Configuration
PORT=8000
HOST=0.0.0.0
